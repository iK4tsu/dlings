/// Variables are mutable by default in D. This means that their value can be
/// changed at any time.

/// Run `dlings hint variables1` or type 'hint' if in watch mode.

// Just D it

unittest
{
	// Without modifying existing code, define logic that will allow the
	// expression `i == 4` to be true.
	int i;
	assert(i == 4);
}
