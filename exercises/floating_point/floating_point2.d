/// Remeber: the README files contain a 'Further reading' section to help you
/// in each chapter.
/// Run `dlings hint variables1` or type 'hint' if in watch mode.

// Just D it

unittest
{
	float f; // Modify this line only!
	f += 0.5;
	assert(f == 0.5);
}
